- 实现对服务器地址 SRV 记录的解析。对于如 `example.net` 这样的简单地址，DNS 中该域名的 SRV 记录会提供服务器的地址和端口。
详见：https://wiki.mumble.info/wiki/SRV_Record 。

- 修复触发角按键发言的权限请求，不再崩溃。

- 在证书确认对话框也显示 SHA-256 摘要，并使文字可选择。

- 由 Allan Nordhøy 润色英语源字符串。

- 由 R4SAS 完整翻译 Mumla 至俄语。

- 移除总是有问题的发言状态动画。

- 在悬浮窗中使用与主界面相同的用户状态图标。

- 修正暗黑主题下的令牌分段图标
